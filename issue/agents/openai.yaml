interface:
  display_name: "Issue Workspace Manager"
  short_description: "Manage sessions, branches, worktrees, and local rules"
  default_prompt: "Use $issue to manage the current task session, branch, worktree, and shared local project rules."
