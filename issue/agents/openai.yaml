interface:
  display_name: "Issue Workspace Manager"
  short_description: "Manage task states, branches, worktrees, and local rules"
  default_prompt: "Use $issue to manage the current task state, branch, worktree, and shared local project rules."
